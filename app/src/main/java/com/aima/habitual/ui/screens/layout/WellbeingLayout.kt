package com.aima.habitual.ui.screens.layout

import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp

/** Screen-specific dimensions for WellBeingScreen. */
object WellbeingLayout {
    // No screen-specific overrides yet — uses foundation tokens.
    // Future: custom ring offsets, stat grid sizing, etc.
}
